{
  "name": "media-review-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cy:open:local": "cross-env CYPRESS_CONFIG_FILE=config/local.json cypress open",
    "cy:run:local": "cross-env CYPRESS_CONFIG_FILE=config/local.json cypress run",
    "cy:run:prod": "cross-env CYPRESS_CONFIG_FILE=config/production.json cypress run --browser chrome --headless"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "cypress": "^15.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
